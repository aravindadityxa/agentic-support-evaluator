[pytest]
# Pytest configuration - minimal to prevent hanging
python_files = test_*.py
python_classes = Test*
python_functions = test_*

timeout = 5
addopts = --tb=line -p no:cacheprovider -p no:warnings

# Skip problematic test collections that cause hanging
# Use unittest directly instead
testpaths = tests

